# Spring Boot Configuration Template
# This is a template file for AI reference - no actual secrets

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Application Info
spring.application.name=nyyu-api

# Jackson Configuration
spring.jackson.property-naming-strategy=SNAKE_CASE
spring.jackson.default-property-inclusion=NON_NULL

# Logging Configuration
logging.level.root=INFO
logging.level.com.nyyu=DEBUG

# Profile Configuration
spring.profiles.active=dev

# Database Configuration Template (use environment variables)
# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}

# Security Configuration Template
# jwt.secret=${JWT_SECRET}
# jwt.expiration=${JWT_EXPIRATION:86400000}
